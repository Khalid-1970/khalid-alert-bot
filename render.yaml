services:
  - type: worker
    name: khalid-alert-bot
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python main.py

    plan: free

    autoDeploy: true
